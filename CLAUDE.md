@AGENTS.md

## Claude Code

- Use the project `acr` MCP server as external persistent intelligence.
- The `UserPromptSubmit` hook may add bounded, authority-free ACR evidence.
- The `Stop` hook requests one small learning-candidate review after verified
  coding work; it never authorizes a memory write.
- Keep durable project facts in governed ACR memory, not Claude auto-memory or
  an expanding `CLAUDE.md`.
- Use `/mcp` to inspect the provider and `/memory` to confirm this file loaded.
