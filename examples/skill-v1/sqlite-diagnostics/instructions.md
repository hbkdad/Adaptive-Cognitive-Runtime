# SQLite diagnostics

Inspect only the requested local database. Check schema version, SQLite
integrity, foreign keys, and FTS availability. Report exact evidence and do not
perform repair or migration unless the caller separately authorizes it.
